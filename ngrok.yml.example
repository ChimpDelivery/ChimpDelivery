authtoken:
region: eu
tunnels:
  laravel_tunnel:
    proto: http
    hostname: dashboard.talusstudio.eu.ngrok.io
    addr: 127.0.0.1:80

    # -------------------------
    # Additional options
    # -------------------------
    # auth: "username:password"
    # host_header: rewrite
    # inspect: true
    # bind_tls: true
